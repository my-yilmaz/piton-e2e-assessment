function Page () {
}

Page.prototype.open = function (path) {
    var url = buildUrl(path);
    browser.url(url);
};

function buildUrl(path){
  if (!path) {
    var url = '/';
    return url;
  } else {
    var url = '/' + path;
    return url;
  };
};

module.exports = new Page()
